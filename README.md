# Astral Chart Wheel

Shared deterministic SVG chart-wheel rendering components for Online Arcana projects.

This repository is intentionally limited to visual chart-wheel rendering and its canonical glyph assets. It does not calculate astronomical positions and it does not encode identities.

The initial extraction preserves the behaviour of the existing `Online-Arcana/astrology` chart wheel. Consumers pin this repository as a Git submodule.
