name: tmp-sync-dist
on:
  push:
    branches: [agent/split-modules]
    paths: [.github/workflows/tmp-sync-dist.yml]
permissions:
  contents: write
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/split-modules
          submodules: recursive
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - name: Build and commit exact dist
        run: |
          set -euo pipefail
          npm run vendor:build
          npm ci
          npm run check
          npm run build
          npm test
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -f dist
          git commit -m "Regenerate astral-core distribution"
          git push origin HEAD:agent/split-modules
